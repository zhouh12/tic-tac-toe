/**
 * Configuration for the Tic Tac Toe Component.
 */
export interface TicTacToeConfig {
  /**
   * Don't really have config for this example
   * But in real world component might have some configurations
   */
}
